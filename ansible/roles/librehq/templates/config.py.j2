# This file is generated from the ansible setup script
#
# If it needs to be customized differently, you should do the customizations there.

SQLALCHEMY_DATABASE_URI='postgresql://librehq:{{ db_password }}@localhost/librehq_core'
SQLALCHEMY_BINDS = {
    "wikis": 'postgresql://librehq:{{ db_password }}@localhost/librehq_wikis'
}
WIKI_URL = 'wiki.{{ wiki_hostname }}'

MW_CONTROL_USER_PW = '{{ wikiuser_db_pass }}'
